[package]
name = "workspace"
version = "0.1.0"
authors = ["Daniel Collin <daniel@collin.com>"]

# Type
[lib]
name = "workspace"
crate-type = ["dylib"]

# Depends
[dependencies]
prodbg_api = { path = "../../../api/rust/prodbg" }
walkdir = "0.1"
